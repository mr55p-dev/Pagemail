---
name: Deploy code from master to Production

on:
  workflow_dispatch:
  pr:

permissions:
  contents: read

concurrency:
  group: production
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - uses: actions/setup-go@v4
        with:
          go-version: "1.21.5"
          cache: true
          cache-dependency-path: server/go.sum

      - name: Install dependencies
        run: make install

      - name: Build binary
        run: make build

      - name: Store build artifact
        uses: actions/upload-artifact@master
        with:
          name: build
          path: ${{ github.workspace }}/tmp/main

  deploy:
    runs-on: ubuntu-latest
    environment: Production
    needs: [build]

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Download backend
        uses: actions/download-artifact@v3
        with:
          name: build
          path: build/

      - name: Clean environment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          script: rm -rf /home/ec2-user/pagemail

      - name: Copy build artifact onto server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          source: "build/*"
          target: /home/ec2-user/prd/pagemail/
          strip_components: 1

      - name: Copy server files onto server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          source: "install.sh,nginx/*,services/*,test_pages/*"
          target: /home/ec2-user/pagemail

      - name: Run install script
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.host }}
          username: ${{ secrets.username }}
          key: ${{ secrets.key }}
          port: ${{ secrets.port }}
          script: sudo /home/ec2-user/pagemail/install.sh /home/ec2-user/pagemail prd

from setuptools import setup

setup(name='async-scheduler',
      version='0.0.1',
      description='A function based scheduler based on the async event loop.',
      author='Ellis Lunnon',
      author_email='ellislunnon@gmail.com',
      license='MIT',
      packages=['async_scheduler'],
      zip_safe=False)
